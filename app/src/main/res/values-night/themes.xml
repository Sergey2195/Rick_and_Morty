<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.RickAndMorty" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/variantFirst</item>
        <item name="colorPrimaryVariant">@color/variantSecond</item>
        <item name="colorOnPrimary">@color/backgroundDark</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/variantFirst</item>
        <item name="colorSecondaryVariant">@color/variantSecond</item>
        <item name="colorOnSecondary">@color/backgroundDark</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
        <item name="selectedItemColor">@color/white</item>
        <item name="unselectedItemColor">@color/grey</item>
    </style>
    <style name="Theme.RickAndMorty.NoStandardActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="BottomNavigationTheme" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/grey</item>
    </style>

    <style name="CustomBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/CustomBottomSheetStyle</item>
    </style>

    <style name="CustomBottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>
</resources>